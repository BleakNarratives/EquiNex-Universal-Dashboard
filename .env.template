# EquiNex-Universal-Dashboard — environment template
# Copy to .env and fill in. .env is gitignored — never commit a real key.
#
# GEMINI_API_KEY powers the live AI voice assistant (services/aiService.ts,
# injected by vite.config.ts as process.env.API_KEY).
#
# Get a key: https://aistudio.google.com/apikey  (create with your Google
# account — an OAuth token from the vault will NOT work; it must start
# with "AIza").
GEMINI_API_KEY=
